name: 🚀 Deploy to Production

on:
  push:
    branches: [ main, master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 📁 Verify files exist
      run: |
        if [ -f "./docker/Dockerfile" ]; then
          echo "✅ Dockerfile found at ./docker/Dockerfile"
        else
          echo "❌ Dockerfile not found at ./docker/Dockerfile"
          exit 1
        fi
        
        if [ -f "docker/docker-compose.yml" ]; then
          echo "✅ docker-compose.yml found"
        else
          echo "❌ docker-compose.yml not found"
          exit 1
        fi

    - name: 🔐 Setup SSH
      run: |
        mkdir -p ~/.ssh
        chmod 700 ~/.ssh
        echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts 2>/dev/null

    - name: 🏗️ Build Docker image
      run: |
        docker build -t spectrum-club-bot:${{ github.sha }} -f ./docker/Dockerfile .
        docker save spectrum-club-bot:${{ github.sha }} | gzip > bot-image.tar.gz
      env:
        DOCKER_BUILDKIT: 1
        BUILDKIT_PROGRESS: plain

    - name: 📝 Create .env file locally
      run: |
        # Создаем .env файл
        cat > .env << 'EOF'
        DB_USER=${{ secrets.DB_USER }}
        DB_PASSWORD=${{ secrets.DB_PASSWORD }}
        DB_NAME=${{ secrets.DB_NAME }}
        BOT_TOKEN=${{ secrets.BOT_TOKEN }}
        DB_SSL_MODE=disable
        LOG_LEVEL=info
        BOT_DEBUG=false
        ENVIRONMENT=${{ secrets.ENVIRONMENT }}
        BASE_URL=${{ secrets.BASE_URL }}
        IMAGE_TAG=${{ github.sha }}
        HTTPS_PROXY=${{ secrets.HTTPS_PROXY }}
        HTTP_PROXY=${{ secrets.HTTP_PROXY }}
        NO_PROXY=${{ secrets.NO_PROXY }}
        EOF
        
        echo "✅ .env file created"
        echo "📄 .env content (keys only):"
        cat .env | sed 's/=.*/=[HIDDEN]/'

    - name: ✅ Validate BASE_URL format
      run: |
        BASE_URL_RAW="${{ secrets.BASE_URL }}"
        if [ -z "$BASE_URL_RAW" ]; then
          echo "❌ BASE_URL secret is empty"
          exit 1
        fi

        # Must be scheme + host only (optional trailing slash), without path/query/fragment.
        if [[ ! "$BASE_URL_RAW" =~ ^https?://[^/]+/?$ ]]; then
          echo "❌ BASE_URL must be domain-only URL (example: https://spectrumbot.ru)"
          echo "Current value has an extra path or invalid format."
          exit 1
        fi

        echo "✅ BASE_URL format is valid"

    - name: 📦 Copy files to server
      run: |
        scp -o StrictHostKeyChecking=no \
            bot-image.tar.gz \
            docker/docker-compose.yml \
            .env \
            ${{ secrets.SSH_USER }}@${{ secrets.SERVER_HOST }}:/opt/spectrum-bot/
        # Копируем веб-шаблоны рекурсивно
        # scp -r -o StrictHostKeyChecking=no \
        #     web/ \
        #     ${{ secrets.SSH_USER }}@${{ secrets.SERVER_HOST }}:/opt/spectrum-bot/
        echo "✅ All files copied successfully"

    - name: 🚀 Deploy on server
      run: |
        ssh -o StrictHostKeyChecking=no \
            ${{ secrets.SSH_USER }}@${{ secrets.SERVER_HOST }} "
          set -e
          
          echo '📦 Loading Docker image...'
          cd /opt/spectrum-bot
          docker load -i bot-image.tar.gz
          
          echo '🔍 Getting PostgreSQL container IP...'
          # Получаем IP адрес контейнера postgres-db-spectrum
          POSTGRES_IP=\$(docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' postgres-db-spectrum)
          echo \"PostgreSQL IP: \${POSTGRES_IP}\"
          
          echo '📝 Updating .env with PostgreSQL IP...'
          # Обновляем .env файл с IP вместо имени хоста
          sed -i \"s|DB_HOST=.*|DB_HOST=\${POSTGRES_IP}|\" .env
          echo \"Updated DB_HOST to: \${POSTGRES_IP}\"
          
          echo '🔄 Updating docker-compose.yml with new tag...'
          sed -i 's|image: spectrum-club-bot:.*|image: spectrum-club-bot:${{ github.sha }}|' docker-compose.yml
          
          echo '🚀 Starting bot...'
          docker compose down --remove-orphans 2>/dev/null || true
          docker compose --env-file .env up -d
          
          echo '⏳ Waiting for bot to start...'
          sleep 10

          echo '🧪 Running smoke checks...'
          # Internal checks do not depend on external TLS/reverse proxy config.
          curl -fsS "http://localhost:8080/health" > /dev/null
          curl -fsS "http://localhost:8080/calendar" > /dev/null
          curl -fsS "http://localhost:8080/api/auth/status" > /dev/null
          echo '✅ Internal smoke checks passed'
          
          # Optional external check (non-blocking): verifies domain/reverse-proxy path.
          BASE_URL_CLEAN=\$(grep '^BASE_URL=' .env | cut -d '=' -f2 | sed 's#/*\$##')
          if [ -n "\${BASE_URL_CLEAN}" ]; then
            if curl -fsS "\${BASE_URL_CLEAN}/health" > /dev/null; then
              echo '✅ External health check passed'
            else
              echo '⚠️ External health check failed (TLS/proxy/domain issue), deploy continues'
            fi
          fi
          
          echo '🔍 Bot logs:'
          docker compose logs --tail=20 bot
        "